# PROJECT_KNOWLEDGE

このファイルは、新しいチャットがプロジェクトの前提・判断・運用知見を読むだけで経緯を把握できるようにするための共有ナレッジです。

## 配下プロジェクト: tottori-mieru

- `tottori-mieru` は、地方議会見える化の全国展開に向けた最初の実装系。鳥取県内5議会から始まり、都道府県議会・市町村議会を同じデータ構造で扱う方向に拡張中。
- 議員写真は全議会で持たない方針に決定。
  - 理由: 写真は元々ホットリンクだった。具体的には `members.json` の `photo_url` を `<img src>` に入れ、ユーザーのブラウザが相手自治体サーバーから直接読む構造だった。
  - 都道府県から市町村へ全国展開すると、相手サイト改装によるURL切れ、写真が突然消えても自動更新で気づけない問題、相手サーバーへの継続負荷、robots禁止パス参照などの管理コストがスケールしない。
  - 福岡県議会では、議員ページHTMLは参照可能でも写真が `/giin/` 配下にあり robots.txt で Disallow されていた。この種の例外を全国規模で個別管理し続けるのは本質的ではない。
  - 写真は議員データの本質ではない。プロジェクトの核は「誰が・どの会派で・何に賛否を示したか」であり、議員ページのシンプル化でもこの原則を確立した。
  - よって写真表示は全廃し、全議会を写真なしカードで統一する。カードは氏名・会派・選挙区・当選回数を中心にする。
  - `photo_url` フィールドは既存データ互換用に当面残すが、表示には一切使わない。
  - 新規スクレイパーは `photo_url` 取得を省略し、`null` を出力する。

## 9章 運用で得た知見

- robots.txt の構造的知見:
  - 議員ページHTMLは許可パス(`/site/giin/` など)にある一方で、写真画像だけ禁止パス(`/giin/` など)に置く自治体がある。確認済みの例として福岡県議会、同種の注意例として東京がある。
  - robots.txt は強制力のないクローラー向け指示書だが、当プロジェクトは尊重する方針。
  - 写真全廃により、写真パスの robots 制約問題自体が全国展開から消えた。写真を取得せず、表示にも使わないため。
- ホットリンクの教訓:
  - 外部画像URLを `<img src>` で直接参照する構造は、相手サイト依存で「いつの間にか壊れる」不安定さを持つ。
  - 全国規模でスケールしないものは、本質でなければ持たない判断が有効。
